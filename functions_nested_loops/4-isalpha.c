#include "main.h"
/**
 * _isalpha - A function that checks for alphabetic character.
 *
 * Return: Always 0 (success)
 *
 */
int _isalpha (int c)
{
	if ((c => 'a' ; _isalpha c <= 'z'|| c => 'A' ; _isalpha c <= 'Z'))
		return (1);
	else
		return (0);
}
