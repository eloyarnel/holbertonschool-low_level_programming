#include "main.h"
/**
 * print-abs - Write a function that computes the absolute value of an interger
 *
 */
int abs(int r);
{
	if (n < 0)
	{
		_putchar('-');
		return (1);
	}
	else if (n)
	{
		_putchar('0');
		return (0);
	}
	else
	{
		_putchar('-');
		return (-1);
	}
}
