#include <stdio.h>
#include <stdlib.h>
/**
 * mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
