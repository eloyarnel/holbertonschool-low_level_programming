#include "main.h"

/**
 * int mul - a function that multiplies two intergers
 * @a @b: The character check
 *
 *Return: Always 0
 */
int mul(int a, int b)
{
	if (a >= '0' && b <= '9')
		return (1);
	else
		return (0);
}
