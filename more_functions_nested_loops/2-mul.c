#include "main.h"

/**
 * int mul - a function that multiplies two intergers
 * @a @b: The character check
 *
 *Return: The producct of a & b
 */
int mul(int a, int b)
{
	if (a >= '0' && b <= '9')
		return (a * b);
}
