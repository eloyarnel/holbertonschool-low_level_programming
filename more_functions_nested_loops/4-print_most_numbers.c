#include "main.h"

/**
 * main - Prints the number from 0 to 9, followed by a new line 
 *
 *
 * Return: Always 0 (Success)
 *
 */
int void print_most_numbers(void)
{
	char number;

	for (number = '0'; number != 4 ; number != 2 ; number <= '9';)
		_putchar(number);
	_putchar('\n');

	return (0);
}
